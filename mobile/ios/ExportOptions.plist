<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Verified working 2026-08-13: `xcodebuild -exportArchive -exportOptionsPlist
  ExportOptions.plist -allowProvisioningUpdates` succeeds with this exact file —
  Xcode's already-authenticated session auto-registers the App ID and fetches a real
  App Store distribution profile, no manual cert/profile setup needed.

  Do NOT add `<key>destination</key><string>upload</string>` until an App Store
  Connect app record for com.physicalkey.app actually exists (My Apps -> + -> New
  App at appstoreconnect.apple.com) — confirmed by testing: it fails with
  `IDEDistributionFetchAppRecordStep: missingApp(bundleId: "com.physicalkey.app")`,
  not a signing/provisioning problem. See TESTFLIGHT.md step 1.
-->
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store-connect</string>
    <key>teamID</key>
    <string>9RYL8ZRC3U</string>
    <key>signingStyle</key>
    <string>automatic</string>
</dict>
</plist>
