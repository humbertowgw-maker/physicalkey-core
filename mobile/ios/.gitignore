# Regenerated by `xcodegen generate` from project.yml — not hand-edited, but committed
# alongside project.yml for convenience (open directly in Xcode without installing xcodegen).
Generated-Info.plist

# User-specific Xcode state — never shared/committed
xcuserdata/
*.xcuserstate
DerivedData/
.build/
